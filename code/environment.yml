name: emotion
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy>=1.24
  - pandas>=2.0
  - scikit-learn>=1.3
  - joblib>=1.3
  - matplotlib>=3.7
  - pytorch>=2.1      # CPU build; see README for GPU install
  - pip:
      - datasets>=2.14
      - transformers>=4.35
      - accelerate>=0.24
      - streamlit>=1.28
      - altair>=5.0
